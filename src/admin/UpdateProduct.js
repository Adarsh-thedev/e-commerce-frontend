import React from 'react';
import Base from '../core/Base';

const UpdateProduct = () => {
    return(
        <Base>
            Update Product Here(TODO)
        </Base>
    );
}

export default UpdateProduct;